/**
 * Core document model of the Information Retrieval engine.
 * <p>
 * {@link codex.ir.Document} is the fundamental data unit, carrying
 * raw and normalized content, structured fields, and metadata for indexing.
 * {@link codex.ir.Document.DocumentMetadata} provides strongly-typed and
 * extensible per-document statistics (length, term frequencies, attributes).
 * </p>
 */
package codex.ir;
