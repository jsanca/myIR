/**
 * Global term vocabulary for vector-space retrieval.
 * <p>
 * {@link codex.ir.corpus.vector.Vocabulary} maps normalized terms to stable
 * integer dimension identifiers, supporting forward (term → id) and reverse
 * (id → term) lookups. Used by {@link codex.ir.vector.Vectorizer} and
 * {@link codex.ir.vector.store.DocumentVectorStore}.
 * </p>
 */
package codex.ir.corpus.vector;
