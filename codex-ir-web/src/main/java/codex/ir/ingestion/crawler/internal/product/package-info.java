/**
 * Internal product extraction logic.
 * <p>
 * Contains JSON-LD extraction ({@code JsonLdProductExtractor}), price parsing
 * ({@code ProductPriceParser}), image resolution ({@code ImageUrlResolver}),
 * card classification ({@code CardClassifier}), extraction quality assessment
 * ({@code ExtractionWarning}, {@code ExtractionQualitySummary}), and supporting
 * types for per-page extraction results.
 * This package is <b>internal</b> and not JPMS-exported.
 * </p>
 */
package codex.ir.ingestion.crawler.internal.product;
