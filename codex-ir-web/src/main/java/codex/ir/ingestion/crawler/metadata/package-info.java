/**
 * HTML page metadata extraction.
 * <p>
 * {@link codex.ir.ingestion.crawler.metadata.PageMetadataExtractor} extracts
 * rich metadata from a fetched {@link codex.ir.ingestion.WebPage}:
 * title, description, canonical URL, OpenGraph tags, Twitter Card tags,
 * robots directives, headings, language, and JSON-LD blocks. Results are
 * captured in {@link codex.ir.ingestion.crawler.metadata.PageMetadata}.
 * </p>
 */
package codex.ir.ingestion.crawler.metadata;
